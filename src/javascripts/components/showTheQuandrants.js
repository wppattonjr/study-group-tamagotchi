/* import buildQuadrants from './buildQuadrants';

const showQuadrants = (array) => {
  $('#eat').html('');
  $('#play').html('');
  $('#fight').html('');
  $('#sleep').html('');
  array.forEach((actions) => {
    if (actions.name === 'eat') {
      $('#eat').append(buildQuadrants.buildQuandrants(actions.name, actions.button1, actions.button2, actions.score));
    } else if (actions.name === 'play') {
      $('#play').append(buildQuadrants.buildQuandrants(actions.name, actions.button1, actions.button2, actions.score));
    } else if (actions.name === 'fight') {
      $('#fight').append(buildQuadrants.buildQuandrants(actions.name, actions.button1, actions.button2, actions.score));
    } else {
      $('#sleep').append(buildQuadrants.buildQuandrants(actions.name, actions.button1, actions.button2, actions.score));
    }
  });
};

export default { showQuadrants };
 */
